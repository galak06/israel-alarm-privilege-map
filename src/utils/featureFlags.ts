// Feature flags — flip to `true` once the data/API is ready.

/** Show per-city alert/siren counts in the city info panel and include
 *  safetyScore in the privilege score calculation.
 *  Requires REDALERT_API_KEY to be set when running fetch-localities.mjs. */
export const ALERTS_ENABLED = false;
